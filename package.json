{
  "name": "jansoochna",
  "version": "1.0.0",
  "description": "Hyperlocal civic utility and accountability app for Indian citizens",
  "main": "server/src/index.js",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client && npm install --prefix admin",
    "dev": "npx concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\" \"npm run dev --prefix admin\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "admin": "npm run dev --prefix admin",
    "build-client": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
