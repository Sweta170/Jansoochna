# Backend

Node.js + Express backend with Socket.IO. This is a minimal scaffold.

Quick start:

```powershell
cd backend
npm install
npm run start
```

Endpoints:
- `GET /api/health`
- `GET /api/complaints`
- `POST /api/complaints`
