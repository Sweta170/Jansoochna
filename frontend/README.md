# Frontend (React)

Minimal Vite + React frontend that demonstrates authentication, creating complaints, and real-time updates via Socket.IO.

Quick start:

```powershell
cd frontend
npm install
npm run dev
```
# Frontend

Placeholder for React frontend. Use Create React App or Vite.

Suggested quick start:

```powershell
cd frontend
npm init vite@latest
npm install
npm run dev
```
